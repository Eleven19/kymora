name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: Release version without the leading v
        required: true
        type: string

permissions:
  contents: write

env:
  GIT_CLIFF_VERSION: "2.12.0"

jobs:
  metadata:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.metadata.outputs.version }}
      tag: ${{ steps.metadata.outputs.tag }}
      prerelease: ${{ steps.metadata.outputs.prerelease }}
      release_name: ${{ steps.metadata.outputs.release_name }}
    steps:
      - uses: actions/checkout@v4

      - id: metadata
        uses: ./.github/actions/release-metadata
        with:
          version: ${{ github.event.inputs.version }}

  build:
    needs: metadata
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "25"

      - uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Setup Mill caches
        uses: ./.github/actions/setup-mill-caches

      - name: Install Scala Native toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y clang libunwind-dev libgc-dev

      - name: Compile JVM modules
        run: ./mill kymora.vfs.jvm.compile + kymora.workflow.jvm.compile + kymora.workflow-testkit.jvm.compile + kymora.kyo.mill.jvm.compile + kymora.examples.jvm.compile

      - name: Run JVM tests
        run: ./mill kymora.vfs.jvm.test + kymora.workflow.jvm.test + kymora.workflow-testkit.jvm.test + kymora.kyo.mill.jvm.test + kymora.examples.jvm.test

      - name: Compile Scala.js modules
        run: ./mill kymora.vfs.js.compile + kymora.workflow.js.compile + kymora.workflow-testkit.js.compile

      - name: Run Scala.js tests
        run: ./mill kymora.vfs.js.test + kymora.workflow.js.test + kymora.workflow-testkit.js.test

      - name: Compile Scala.js WASM modules
        run: ./mill kymora.vfs.wasm.compile + kymora.workflow.wasm.compile + kymora.workflow-testkit.wasm.compile

      - name: Run Scala.js WASM tests
        run: ./mill kymora.vfs.wasm.test + kymora.workflow.wasm.test + kymora.workflow-testkit.wasm.test

      - name: Compile Scala Native modules
        run: ./mill kymora.vfs.native.compile + kymora.workflow.native.compile + kymora.workflow-testkit.native.compile

      - name: Run Scala Native tests
        run: ./mill kymora.vfs.native.test + kymora.workflow.native.test + kymora.workflow-testkit.native.test

      - name: Run Kyo Mill plugin integration tests
        run: ./mill kymora.kyo.mill.jvm.integration

  publish:
    needs:
      - metadata
      - build
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
      RELEASE_TAG: ${{ needs.metadata.outputs.tag }}
      RELEASE_NAME: ${{ needs.metadata.outputs.release_name }}
      RELEASE_PRERELEASE: ${{ needs.metadata.outputs.prerelease }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Create tag for manual release
        if: github.event_name == 'workflow_dispatch'
        shell: bash
        run: scripts/ci/create-release-tag.sh

      - name: Install git-cliff
        shell: bash
        run: scripts/ci/install-git-cliff.sh "${{ env.GIT_CLIFF_VERSION }}"

      - name: Build release notes
        id: release-notes
        shell: bash
        run: scripts/ci/build-release-notes.sh "${{ needs.metadata.outputs.version }}"

      - name: Create or update GitHub release
        shell: bash
        env:
          RELEASE_NOTES_FILE: ${{ steps.release-notes.outputs.release_notes_file }}
        run: scripts/ci/create-or-update-github-release.sh
